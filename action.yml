name: External LLM Vulnerability Scan
description: Scan PR diffs for vulnerabilities using external LLM-based service.

inputs:
  base_ref:
    description: Base branch to compare
    required: true
  head_ref:
    description: Head branch to compare
    required: true
  code_advice_url:
    description: URL of the external code scanning service
    required: true

runs:
  using: "composite"
  steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Fetch base and head branches, get diff (excluding workflow files)
      id: diff
      shell: bash
      run: |
        git fetch origin ${{ inputs.base_ref }}
        git fetch origin ${{ inputs.head_ref }}
        
        DIFF=$(git diff --unified=0 origin/${{ inputs.base_ref }} origin/${{ inputs.head_ref }} -- . ':(exclude).github/workflows/**')
        
        if [ -z "$DIFF" ]; then
          echo "skip=true" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        
        echo "$DIFF" > diff.patch
        echo "patch<<EOF" >> "$GITHUB_OUTPUT"
        cat diff.patch >> "$GITHUB_OUTPUT"
        echo "EOF" >> "$GITHUB_OUTPUT"


    - name: Send diff to external vulnerability service
      if: steps.diff.outputs.skip != 'true'
      id: advice
      shell: bash
      run: |
        echo "${{ steps.diff.outputs.patch }}" > diff.patch
        
        RESPONSE=$(curl -sS \
          -X POST "${{ inputs.code_advice_url }}" \
          -H "Accept: text/plain" \
          -H "Content-Type: text/plain" \
          --data-binary @diff.patch)
        
        echo "$RESPONSE" > scan.txt
        echo "text<<EOF" >> "$GITHUB_OUTPUT"
        cat scan.txt >> "$GITHUB_OUTPUT"
        echo "EOF" >> "$GITHUB_OUTPUT"

    - name: Save scan output to file
      if: steps.diff.outputs.skip != 'true'
      shell: bash
      run: |
        echo "${{ steps.advice.outputs.text }}" > scan.txt

    - name: Fail build on detected vulnerabilities
      if: steps.diff.outputs.skip != 'true'
      shell: bash
      run: |
        if grep -q 'Issue' scan.txt; then
          echo "🚨 Vulnerabilities detected! Failing the build."
          cat scan.txt
          exit 1
        else
          echo "✅ No vulnerabilities found, build OK."
        fi
